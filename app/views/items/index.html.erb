<h1>Items</h1>
<div class="container-item1">
<% @items.each do |item| %>
	<div class="container-item0">	
	  <ul>
	  	<p>
	  	<%= item.nombre %>
	  	<p>
	  	<%= image_tag item.foto.url(:medium) %>
	  	<p>
		<a href=<%= edit_item_path(item)%> >Editar</a>
	  </ul>
	</div>
<% end %>
</div>