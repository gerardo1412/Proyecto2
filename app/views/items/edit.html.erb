<h1>Retirar item</h1>
 
<%= form_for :item, url: item_path(@item), method: :patch do |f| %>
  <% if @item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@item.errors.count, "error") %> prohibited
      this item from being saved:</h2>
    <ul>
    <% @item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <h3>Ahora hay : </h3>   <%= @item.cantidad %>

  <p>
    <%= f.label :Modificar_Cantidad %><br>
    <%= f.text_field :cantidad %>
  <p>
  
    <%= f.hidden_field :usados , value: @item.cantidad  %>


  
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', items_path %>